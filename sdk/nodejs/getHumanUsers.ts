// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Datasource representing human users situated under an organization, which then can be authorized through memberships or direct grants on other resources.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as zitadel from "@scoretechnologies/zitadel";
 *
 * const _default = zitadel.getHumanUsers({
 *     orgId: defaultZitadelOrg.id,
 *     userName: "example-name",
 *     userNameMethod: "TEXT_QUERY_METHOD_CONTAINS_IGNORE_CASE",
 * });
 * const defaultGetHumanUser = _default.then(_default => std.toset({
 *     input: _default.userIds,
 * })).then(invoke => .reduce((__obj, [__key, __value]) => ({ ...__obj, [String(__key)]: zitadel.getHumanUser({
 *     orgId: defaultZitadelOrg.id,
 *     userId: __value,
 * }) }), {}));
 * export const userNames = defaultGetHumanUser.apply(defaultGetHumanUser => std.toset({
 *     input: Object.values(defaultGetHumanUser).map(user => (user.userName)),
 * })).apply(invoke => invoke.result);
 * ```
 */
export function getHumanUsers(args?: GetHumanUsersArgs, opts?: pulumi.InvokeOptions): Promise<GetHumanUsersResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("zitadel:index/getHumanUsers:getHumanUsers", {
        "displayName": args.displayName,
        "displayNameMethod": args.displayNameMethod,
        "email": args.email,
        "emailMethod": args.emailMethod,
        "firstName": args.firstName,
        "firstNameMethod": args.firstNameMethod,
        "lastName": args.lastName,
        "lastNameMethod": args.lastNameMethod,
        "loginName": args.loginName,
        "loginNameMethod": args.loginNameMethod,
        "nickName": args.nickName,
        "nickNameMethod": args.nickNameMethod,
        "orgId": args.orgId,
        "userName": args.userName,
        "userNameMethod": args.userNameMethod,
    }, opts);
}

/**
 * A collection of arguments for invoking getHumanUsers.
 */
export interface GetHumanUsersArgs {
    /**
     * Display name to filter by
     */
    displayName?: string;
    /**
     * Method for querying users by display name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    displayNameMethod?: string;
    /**
     * Email to filter by
     */
    email?: string;
    /**
     * Method for querying users by email, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    emailMethod?: string;
    /**
     * First name to filter by
     */
    firstName?: string;
    /**
     * Method for querying users by first name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    firstNameMethod?: string;
    /**
     * Last name to filter by
     */
    lastName?: string;
    /**
     * Method for querying users by last name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    lastNameMethod?: string;
    /**
     * Login name to filter by
     */
    loginName?: string;
    /**
     * Method for querying users by login name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    loginNameMethod?: string;
    /**
     * Nick name to filter by
     */
    nickName?: string;
    /**
     * Method for querying users by nick name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    nickNameMethod?: string;
    /**
     * ID of the organization
     */
    orgId?: string;
    /**
     * Username to filter by
     */
    userName?: string;
    /**
     * Method for querying users by username, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    userNameMethod?: string;
}

/**
 * A collection of values returned by getHumanUsers.
 */
export interface GetHumanUsersResult {
    /**
     * Display name to filter by
     */
    readonly displayName?: string;
    /**
     * Method for querying users by display name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly displayNameMethod?: string;
    /**
     * Email to filter by
     */
    readonly email?: string;
    /**
     * Method for querying users by email, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly emailMethod?: string;
    /**
     * First name to filter by
     */
    readonly firstName?: string;
    /**
     * Method for querying users by first name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly firstNameMethod?: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Last name to filter by
     */
    readonly lastName?: string;
    /**
     * Method for querying users by last name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly lastNameMethod?: string;
    /**
     * Login name to filter by
     */
    readonly loginName?: string;
    /**
     * Method for querying users by login name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly loginNameMethod?: string;
    /**
     * Nick name to filter by
     */
    readonly nickName?: string;
    /**
     * Method for querying users by nick name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly nickNameMethod?: string;
    /**
     * ID of the organization
     */
    readonly orgId?: string;
    /**
     * A list of all user IDs
     */
    readonly userIds: string[];
    /**
     * Username to filter by
     */
    readonly userName?: string;
    /**
     * Method for querying users by username, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly userNameMethod?: string;
}
/**
 * Datasource representing human users situated under an organization, which then can be authorized through memberships or direct grants on other resources.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as zitadel from "@scoretechnologies/zitadel";
 *
 * const _default = zitadel.getHumanUsers({
 *     orgId: defaultZitadelOrg.id,
 *     userName: "example-name",
 *     userNameMethod: "TEXT_QUERY_METHOD_CONTAINS_IGNORE_CASE",
 * });
 * const defaultGetHumanUser = _default.then(_default => std.toset({
 *     input: _default.userIds,
 * })).then(invoke => .reduce((__obj, [__key, __value]) => ({ ...__obj, [String(__key)]: zitadel.getHumanUser({
 *     orgId: defaultZitadelOrg.id,
 *     userId: __value,
 * }) }), {}));
 * export const userNames = defaultGetHumanUser.apply(defaultGetHumanUser => std.toset({
 *     input: Object.values(defaultGetHumanUser).map(user => (user.userName)),
 * })).apply(invoke => invoke.result);
 * ```
 */
export function getHumanUsersOutput(args?: GetHumanUsersOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetHumanUsersResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("zitadel:index/getHumanUsers:getHumanUsers", {
        "displayName": args.displayName,
        "displayNameMethod": args.displayNameMethod,
        "email": args.email,
        "emailMethod": args.emailMethod,
        "firstName": args.firstName,
        "firstNameMethod": args.firstNameMethod,
        "lastName": args.lastName,
        "lastNameMethod": args.lastNameMethod,
        "loginName": args.loginName,
        "loginNameMethod": args.loginNameMethod,
        "nickName": args.nickName,
        "nickNameMethod": args.nickNameMethod,
        "orgId": args.orgId,
        "userName": args.userName,
        "userNameMethod": args.userNameMethod,
    }, opts);
}

/**
 * A collection of arguments for invoking getHumanUsers.
 */
export interface GetHumanUsersOutputArgs {
    /**
     * Display name to filter by
     */
    displayName?: pulumi.Input<string | undefined>;
    /**
     * Method for querying users by display name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    displayNameMethod?: pulumi.Input<string | undefined>;
    /**
     * Email to filter by
     */
    email?: pulumi.Input<string | undefined>;
    /**
     * Method for querying users by email, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    emailMethod?: pulumi.Input<string | undefined>;
    /**
     * First name to filter by
     */
    firstName?: pulumi.Input<string | undefined>;
    /**
     * Method for querying users by first name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    firstNameMethod?: pulumi.Input<string | undefined>;
    /**
     * Last name to filter by
     */
    lastName?: pulumi.Input<string | undefined>;
    /**
     * Method for querying users by last name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    lastNameMethod?: pulumi.Input<string | undefined>;
    /**
     * Login name to filter by
     */
    loginName?: pulumi.Input<string | undefined>;
    /**
     * Method for querying users by login name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    loginNameMethod?: pulumi.Input<string | undefined>;
    /**
     * Nick name to filter by
     */
    nickName?: pulumi.Input<string | undefined>;
    /**
     * Method for querying users by nick name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    nickNameMethod?: pulumi.Input<string | undefined>;
    /**
     * ID of the organization
     */
    orgId?: pulumi.Input<string | undefined>;
    /**
     * Username to filter by
     */
    userName?: pulumi.Input<string | undefined>;
    /**
     * Method for querying users by username, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    userNameMethod?: pulumi.Input<string | undefined>;
}
