// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Datasource representing all identity providers available to an organization, which can be looked up in detail with the type specific IdP datasources.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as zitadel from "@scoretechnologies/zitadel";
 *
 * const _default = zitadel.getOrgIdps({
 *     orgId: defaultZitadelOrg.id,
 *     name: "example-name",
 *     nameMethod: "TEXT_QUERY_METHOD_CONTAINS_IGNORE_CASE",
 *     type: "PROVIDER_TYPE_GOOGLE",
 *     ownerType: "IDP_OWNER_TYPE_ORG",
 * });
 * const defaultGetOrgIdpGoogle = _default.then(_default => std.toset({
 *     input: _default.ids,
 * })).then(invoke => .reduce((__obj, [__key, __value]) => ({ ...__obj, [String(__key)]: zitadel.getOrgIdpGoogle({
 *     orgId: defaultZitadelOrg.id,
 *     id: __value,
 * }) }), {}));
 * export const orgIdpNames = defaultGetOrgIdpGoogle.apply(defaultGetOrgIdpGoogle => std.toset({
 *     input: Object.values(defaultGetOrgIdpGoogle).map(idp => (idp.name)),
 * })).apply(invoke => invoke.result);
 * ```
 */
export function getOrgIdps(args?: GetOrgIdpsArgs, opts?: pulumi.InvokeOptions): Promise<GetOrgIdpsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("zitadel:index/getOrgIdps:getOrgIdps", {
        "name": args.name,
        "nameMethod": args.nameMethod,
        "orgId": args.orgId,
        "ownerType": args.ownerType,
        "type": args.type,
    }, opts);
}

/**
 * A collection of arguments for invoking getOrgIdps.
 */
export interface GetOrgIdpsArgs {
    /**
     * Name of the identity provider.
     */
    name?: string;
    /**
     * Method for querying identity providers by name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    nameMethod?: string;
    /**
     * ID of the organization
     */
    orgId?: string;
    /**
     * Owner type of the identity provider, supported values: IDP*OWNER*TYPE*UNSPECIFIED, IDP*OWNER*TYPE*SYSTEM, IDP*OWNER*TYPE_ORG
     */
    ownerType?: string;
    /**
     * Type of the identity provider, supported values: PROVIDER*TYPE*UNSPECIFIED, PROVIDER*TYPE*OIDC, PROVIDER*TYPE*JWT, PROVIDER*TYPE*LDAP, PROVIDER*TYPE*OAUTH, PROVIDER*TYPE*AZURE*AD, PROVIDER*TYPE*GITHUB, PROVIDER*TYPE*GITHUB*ES, PROVIDER*TYPE*GITLAB, PROVIDER*TYPE*GITLAB*SELF*HOSTED, PROVIDER*TYPE*GOOGLE, PROVIDER*TYPE*APPLE, PROVIDER*TYPE*SAML, PROVIDER*TYPE*ZITADEL
     */
    type?: string;
}

/**
 * A collection of values returned by getOrgIdps.
 */
export interface GetOrgIdpsResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of all identity provider IDs.
     */
    readonly ids: string[];
    /**
     * Name of the identity provider.
     */
    readonly name?: string;
    /**
     * Method for querying identity providers by name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly nameMethod?: string;
    /**
     * ID of the organization
     */
    readonly orgId?: string;
    /**
     * Owner type of the identity provider, supported values: IDP*OWNER*TYPE*UNSPECIFIED, IDP*OWNER*TYPE*SYSTEM, IDP*OWNER*TYPE_ORG
     */
    readonly ownerType?: string;
    /**
     * Type of the identity provider, supported values: PROVIDER*TYPE*UNSPECIFIED, PROVIDER*TYPE*OIDC, PROVIDER*TYPE*JWT, PROVIDER*TYPE*LDAP, PROVIDER*TYPE*OAUTH, PROVIDER*TYPE*AZURE*AD, PROVIDER*TYPE*GITHUB, PROVIDER*TYPE*GITHUB*ES, PROVIDER*TYPE*GITLAB, PROVIDER*TYPE*GITLAB*SELF*HOSTED, PROVIDER*TYPE*GOOGLE, PROVIDER*TYPE*APPLE, PROVIDER*TYPE*SAML, PROVIDER*TYPE*ZITADEL
     */
    readonly type?: string;
}
/**
 * Datasource representing all identity providers available to an organization, which can be looked up in detail with the type specific IdP datasources.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as zitadel from "@scoretechnologies/zitadel";
 *
 * const _default = zitadel.getOrgIdps({
 *     orgId: defaultZitadelOrg.id,
 *     name: "example-name",
 *     nameMethod: "TEXT_QUERY_METHOD_CONTAINS_IGNORE_CASE",
 *     type: "PROVIDER_TYPE_GOOGLE",
 *     ownerType: "IDP_OWNER_TYPE_ORG",
 * });
 * const defaultGetOrgIdpGoogle = _default.then(_default => std.toset({
 *     input: _default.ids,
 * })).then(invoke => .reduce((__obj, [__key, __value]) => ({ ...__obj, [String(__key)]: zitadel.getOrgIdpGoogle({
 *     orgId: defaultZitadelOrg.id,
 *     id: __value,
 * }) }), {}));
 * export const orgIdpNames = defaultGetOrgIdpGoogle.apply(defaultGetOrgIdpGoogle => std.toset({
 *     input: Object.values(defaultGetOrgIdpGoogle).map(idp => (idp.name)),
 * })).apply(invoke => invoke.result);
 * ```
 */
export function getOrgIdpsOutput(args?: GetOrgIdpsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetOrgIdpsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("zitadel:index/getOrgIdps:getOrgIdps", {
        "name": args.name,
        "nameMethod": args.nameMethod,
        "orgId": args.orgId,
        "ownerType": args.ownerType,
        "type": args.type,
    }, opts);
}

/**
 * A collection of arguments for invoking getOrgIdps.
 */
export interface GetOrgIdpsOutputArgs {
    /**
     * Name of the identity provider.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Method for querying identity providers by name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    nameMethod?: pulumi.Input<string | undefined>;
    /**
     * ID of the organization
     */
    orgId?: pulumi.Input<string | undefined>;
    /**
     * Owner type of the identity provider, supported values: IDP*OWNER*TYPE*UNSPECIFIED, IDP*OWNER*TYPE*SYSTEM, IDP*OWNER*TYPE_ORG
     */
    ownerType?: pulumi.Input<string | undefined>;
    /**
     * Type of the identity provider, supported values: PROVIDER*TYPE*UNSPECIFIED, PROVIDER*TYPE*OIDC, PROVIDER*TYPE*JWT, PROVIDER*TYPE*LDAP, PROVIDER*TYPE*OAUTH, PROVIDER*TYPE*AZURE*AD, PROVIDER*TYPE*GITHUB, PROVIDER*TYPE*GITHUB*ES, PROVIDER*TYPE*GITLAB, PROVIDER*TYPE*GITLAB*SELF*HOSTED, PROVIDER*TYPE*GOOGLE, PROVIDER*TYPE*APPLE, PROVIDER*TYPE*SAML, PROVIDER*TYPE*ZITADEL
     */
    type?: pulumi.Input<string | undefined>;
}
