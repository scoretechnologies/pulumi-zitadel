// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Datasource representing all project roles in a project, which can be given as authorizations to users. Note: Group-based filtering is not supported by the ZITADEL API.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as zitadel from "@scoretechnologies/zitadel";
 *
 * const _default = zitadel.getProjectRoles({
 *     orgId: defaultZitadelOrg.id,
 *     projectId: defaultZitadelProject.id,
 *     roleKey: "admin",
 * });
 * export const projectRoles = _default;
 * ```
 */
export function getProjectRoles(args: GetProjectRolesArgs, opts?: pulumi.InvokeOptions): Promise<GetProjectRolesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("zitadel:index/getProjectRoles:getProjectRoles", {
        "displayName": args.displayName,
        "displayNameMethod": args.displayNameMethod,
        "keyMethod": args.keyMethod,
        "orgId": args.orgId,
        "projectId": args.projectId,
        "roleKey": args.roleKey,
    }, opts);
}

/**
 * A collection of arguments for invoking getProjectRoles.
 */
export interface GetProjectRolesArgs {
    /**
     * Display name to filter project roles by
     */
    displayName?: string;
    /**
     * Method for querying project roles by display name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    displayNameMethod?: string;
    /**
     * Method for querying project roles by key, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    keyMethod?: string;
    /**
     * ID of the organization
     */
    orgId?: string;
    /**
     * ID of the project
     */
    projectId: string;
    /**
     * Key to filter project roles by
     */
    roleKey?: string;
}

/**
 * A collection of values returned by getProjectRoles.
 */
export interface GetProjectRolesResult {
    /**
     * Display name to filter project roles by
     */
    readonly displayName?: string;
    /**
     * Method for querying project roles by display name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly displayNameMethod?: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Method for querying project roles by key, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    readonly keyMethod?: string;
    /**
     * ID of the organization
     */
    readonly orgId?: string;
    /**
     * ID of the project
     */
    readonly projectId: string;
    /**
     * Key to filter project roles by
     */
    readonly roleKey?: string;
    /**
     * A list of all project role keys
     */
    readonly roleKeys: string[];
}
/**
 * Datasource representing all project roles in a project, which can be given as authorizations to users. Note: Group-based filtering is not supported by the ZITADEL API.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as zitadel from "@scoretechnologies/zitadel";
 *
 * const _default = zitadel.getProjectRoles({
 *     orgId: defaultZitadelOrg.id,
 *     projectId: defaultZitadelProject.id,
 *     roleKey: "admin",
 * });
 * export const projectRoles = _default;
 * ```
 */
export function getProjectRolesOutput(args: GetProjectRolesOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetProjectRolesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("zitadel:index/getProjectRoles:getProjectRoles", {
        "displayName": args.displayName,
        "displayNameMethod": args.displayNameMethod,
        "keyMethod": args.keyMethod,
        "orgId": args.orgId,
        "projectId": args.projectId,
        "roleKey": args.roleKey,
    }, opts);
}

/**
 * A collection of arguments for invoking getProjectRoles.
 */
export interface GetProjectRolesOutputArgs {
    /**
     * Display name to filter project roles by
     */
    displayName?: pulumi.Input<string | undefined>;
    /**
     * Method for querying project roles by display name, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    displayNameMethod?: pulumi.Input<string | undefined>;
    /**
     * Method for querying project roles by key, supported values: TEXT*QUERY*METHOD*EQUALS, TEXT*QUERY*METHOD*EQUALS*IGNORE*CASE, TEXT*QUERY*METHOD*STARTS*WITH, TEXT*QUERY*METHOD*STARTS*WITH*IGNORE*CASE, TEXT*QUERY*METHOD*CONTAINS, TEXT*QUERY*METHOD*CONTAINS*IGNORE*CASE, TEXT*QUERY*METHOD*ENDS*WITH, TEXT*QUERY*METHOD*ENDS*WITH*IGNORE*CASE
     */
    keyMethod?: pulumi.Input<string | undefined>;
    /**
     * ID of the organization
     */
    orgId?: pulumi.Input<string | undefined>;
    /**
     * ID of the project
     */
    projectId: pulumi.Input<string>;
    /**
     * Key to filter project roles by
     */
    roleKey?: pulumi.Input<string | undefined>;
}
