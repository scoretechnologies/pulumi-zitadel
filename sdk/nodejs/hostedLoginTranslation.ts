// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Organization-level translations for the **hosted login v2** (`/ui/v2/login`) in one language, using the settings/v2 API. **Requires ZITADEL 4.x.** ZITADEL checks the instance-level `iam.policy.read` and `iam.policy.write` permissions for these translations, so the service account needs an instance role such as `IAM_OWNER`; `ORG_OWNER` is not sufficient. Keys that are not set fall back to the instance-level translations and then to the built-in translations of the hosted login. ZITADEL has no API to remove translations, so destroying this resource only removes it from the Terraform state and leaves the last applied translations in place. Instance-level defaults are managed by `zitadel.DefaultHostedLoginTranslation`. The legacy login UI (v1) is customized with `zitadel.LoginTexts`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as zitadel from "@scoretechnologies/zitadel";
 *
 * const _default = new zitadel.HostedLoginTranslation("default", {
 *     orgId: defaultZitadelOrg.id,
 *     language: "en",
 *     translations: JSON.stringify({
 *         loginname: {
 *             title: "Welcome to ACME",
 *             description: "Sign in with your ACME account.",
 *         },
 *     }),
 * });
 * ```
 *
 * ## Import
 *
 * The resource can be imported using the ID format `<language[:org_id]>`, e.g.
 *
 * ```sh
 * $ pulumi import zitadel:index/hostedLoginTranslation:HostedLoginTranslation imported 'en:123456789012345678'
 * ```
 */
export class HostedLoginTranslation extends pulumi.CustomResource {
    /**
     * Get an existing HostedLoginTranslation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: HostedLoginTranslationState, opts?: pulumi.CustomResourceOptions): HostedLoginTranslation {
        return new HostedLoginTranslation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'zitadel:index/hostedLoginTranslation:HostedLoginTranslation';

    /**
     * Returns true if the given object is an instance of HostedLoginTranslation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is HostedLoginTranslation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === HostedLoginTranslation.__pulumiType;
    }

    /**
     * BCP 47 language tag of the translations, e.g. `en`, `de` or `fr-CH`
     */
    declare public readonly language: pulumi.Output<string>;
    /**
     * ID of the organization. If not provided, the organization of the authenticated user/service account is used.
     */
    declare public readonly orgId: pulumi.Output<string>;
    /**
     * Translations as a JSON object with at least one key, for example built with `jsonencode`. The keys follow the structure of the [hosted login locale files](https://github.com/zitadel/zitadel/tree/main/apps/login/locales), e.g. `loginname.title`. Replaces all translations previously set for this language on the organization.
     */
    declare public readonly translations: pulumi.Output<string>;

    /**
     * Create a HostedLoginTranslation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: HostedLoginTranslationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: HostedLoginTranslationArgs | HostedLoginTranslationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as HostedLoginTranslationState | undefined;
            resourceInputs["language"] = state?.language;
            resourceInputs["orgId"] = state?.orgId;
            resourceInputs["translations"] = state?.translations;
        } else {
            const args = argsOrState as HostedLoginTranslationArgs | undefined;
            if (args?.language === undefined && !opts.urn) {
                throw new Error("Missing required property 'language'");
            }
            if (args?.translations === undefined && !opts.urn) {
                throw new Error("Missing required property 'translations'");
            }
            resourceInputs["language"] = args?.language;
            resourceInputs["orgId"] = args?.orgId;
            resourceInputs["translations"] = args?.translations;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(HostedLoginTranslation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering HostedLoginTranslation resources.
 */
export interface HostedLoginTranslationState {
    /**
     * BCP 47 language tag of the translations, e.g. `en`, `de` or `fr-CH`
     */
    language?: pulumi.Input<string | undefined>;
    /**
     * ID of the organization. If not provided, the organization of the authenticated user/service account is used.
     */
    orgId?: pulumi.Input<string | undefined>;
    /**
     * Translations as a JSON object with at least one key, for example built with `jsonencode`. The keys follow the structure of the [hosted login locale files](https://github.com/zitadel/zitadel/tree/main/apps/login/locales), e.g. `loginname.title`. Replaces all translations previously set for this language on the organization.
     */
    translations?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a HostedLoginTranslation resource.
 */
export interface HostedLoginTranslationArgs {
    /**
     * BCP 47 language tag of the translations, e.g. `en`, `de` or `fr-CH`
     */
    language: pulumi.Input<string>;
    /**
     * ID of the organization. If not provided, the organization of the authenticated user/service account is used.
     */
    orgId?: pulumi.Input<string | undefined>;
    /**
     * Translations as a JSON object with at least one key, for example built with `jsonencode`. The keys follow the structure of the [hosted login locale files](https://github.com/zitadel/zitadel/tree/main/apps/login/locales), e.g. `loginname.title`. Replaces all translations previously set for this language on the organization.
     */
    translations: pulumi.Input<string>;
}
